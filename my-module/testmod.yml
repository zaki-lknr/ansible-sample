- name: test my new module
  hosts: localhost
  tasks:
  - name: run the new module
    my_test:
      name: 'hello'
      new: true
      items:
        - foo
        - bar
        - baz
    register: testout
  - name: dump test output
    debug:
      msg: '{{ testout }}'

  - module_base:
      opt: fail me
      state: present
      changed: false
